# {SUBSYSTEM} 依赖关系

> 结构化依赖数据在 `relations/depends_on.yaml` 中维护，本文档补充图谱无法表达的上下文。

## 上游依赖（本系统依赖的外部系统）

### {依赖系统名}

- **依赖类型**: 强依赖 / 弱依赖
- **交互方式**: HTTP / RPC / MQ / DB 直连
- **降级方案**: 
- **历史故障**: 

## 下游依赖（依赖本系统的外部系统）

### {下游系统名}

- **调用接口**: 
- **调用量级**: 
- **影响范围**: 

## 基础设施依赖

### 数据中间件

_(数据库、缓存、消息队列、存储等)_

### 网络层依赖

| 类型 | 依赖项 | 用途 | 故障影响 |
|------|--------|------|---------|
| DNS（内部） | _(如内网 DNS 服务器)_ | 解析内部域名（`*.webank.com`） | |
| DNS（外部） | _(如公网 DNS)_ | 解析外部 API 域名 | |
| 防火墙规则 | _(关键白名单条目)_ | 跨网段访问、出网 | |
| 负载均衡 | _(LB 实例标识)_ | 流量入口 | |
| 出网代理/NAT | _(代理地址或 NAT 网关)_ | 访问外部第三方 API | |
| 跨云/跨 VPC 专线 | _(专线标识)_ | 跨云调用 | |
| 证书 | _(mTLS / TLS 证书)_ | HTTPS、双向认证 | |
| 服务网格 | _(Istio/Linkerd 等)_ | 流量路由、超时控制 | |

> 网络层实体在 `metadata.yaml` 中用 `type: network` 注册，通过 `meta.subtype` 区分具体类型（dns/firewall/lb/proxy/tunnel/cert/mesh）。

## 依赖变更注意事项

_(升级/替换依赖时的兼容性问题和迁移步骤)_
